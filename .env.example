# GitHub Personal Access Token — public_repo read scope only
GITHUB_TOKEN=ghp_your_token_here

# Redis connection string (used by the .NET API)
REDIS_CONNECTION=redis:6379

# Host ports (change if 8080/3000 are already in use)
API_PORT=8080
SVG_PORT=3000
